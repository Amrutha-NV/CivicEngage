# Fetching Service

This folder contains the fetching services for campaign and volunteer data.

- `CampaignFetcher` fetches campaigns from the backend.
- `VolunteerFetcher` fetches volunteers from the backend.

Both services support a dummy method for development and testing.
